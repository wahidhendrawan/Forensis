version: "3.9"

services:
  forensis:
    build: .
    container_name: forensis
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./instance:/app/instance